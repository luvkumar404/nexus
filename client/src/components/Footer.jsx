export default function Footer() {
  return (
    <footer className="border-t border-slate-200 bg-white py-8 text-sm text-slate-500 dark:border-slate-800 dark:bg-slate-950">
      <div className="mx-auto flex max-w-7xl flex-col gap-2 px-4 md:flex-row md:items-center md:justify-between">
        <p>Nexus SEO Auditor</p>
        <p>Backlink and Search Console modules are integration-ready placeholders until provider credentials are configured.</p>
      </div>
    </footer>
  );
}
